import { Button } from 'antd';

const HomePage = () => {
  return (
    <div style={{ padding: '8px' }}>
      <Button type="primary">
        Click me
      </Button>
    </div>
  );
};

export default HomePage;
